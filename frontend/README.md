# SEO 诊断工具 - 前端应用

基于 React + TypeScript + Vite 构建的现代化 SEO 诊断工具前端应用。

## 🎯 项目简介

一款专业的 SEO 自动化诊断系统，支持对任意网站进行一键 SEO 健康检测，提供全方位的诊断报告和优化建议。

## ✨ 核心功能

### 智能诊断系统

---

## SEO诊断功能说明

本项目集成了一套自动化SEO诊断系统，支持对任意网站进行一键SEO健康检测，分析内容涵盖如下四大模块：

### 1. 基础信息分析
- 检查页面的`title`、`description`、`keywords`、`favicon`。
- 检查`robots.txt`和`sitemap.xml`是否存在。
- 缺失项会降低分数并给出优化建议。

### 2. 页面SEO要素分析
- 检查H标签（h1/h2/h3）数量、唯一性和层级顺序。
- 检查图片alt属性缺失情况。
- 检查内外链、nofollow属性、链接文本等。
- 统计页面高频关键词及其密度。

### 3. 技术SEO检测
- 检查移动端适配（viewport标签）。
- 检查结构化数据（Schema.org相关标签）。
- 检查HTTPS/SSL证书。
- 检查页面加载速度。

### 4. 可访问性与内容质量
- 检查是否有自定义404页面。
- 检查死链（前20个链接）。
- 简单评估页面可读性（句长、段落数、总字数等）。

### 5. 综合报告
- 汇总上述四大模块的分数、状态和建议，按权重生成总分。
- 前端支持分模块结果展示和PDF报告导出。

### 技术实现
- **前端**：React + TypeScript + Vite，调用后端 API，展示诊断进度、分数、建议和详细结果
- **后端**：Node.js + Express + Puppeteer，自动化爬取和分析页面，返回结构化诊断结果
- **状态管理**：React Context API 实现全局状态共享
- **路由**：React Router 实现页面导航

## 🚀 快速开始

### 环境要求

- Node.js >= 16.0.0
- npm 或 yarn

### 安装依赖

```bash
cd frontend
npm install
```

### 启动开发服务器

```bash
npm run dev
```

应用将在 **http://localhost:5173** 启动（Vite 默认端口）。

### 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist/` 目录。

### 预览生产构建

```bash
npm run preview
```

## 📦 技术栈

- **React 18** - UI 框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具和开发服务器
- **React Router** - 路由管理
- **Axios** - HTTP 请求库
- **Ant Design** - UI 组件库
- **ESLint** - 代码质量检查

## 📁 目录结构

本项目主要目录结构如下：

```
frontend/
├── public/                # 公共资源目录（静态文件、favicon等）
├── src/                   # 源码目录
│   ├── api/               # 前端API请求封装
│   ├── assets/            # 静态资源（如logo图片等）
│   ├── components/        # 通用组件（如ErrorDrawer、UrlInput等）
│   ├── context/           # React Context（如诊断结果全局状态）
│   ├── pages/             # 页面级组件（如HomePage、ResultPage等）
│   ├── App.tsx            # 应用入口组件
│   ├── main.tsx           # 应用入口文件
│   ├── index.html         # HTML模板
│   ├── index.css          # 全局样式
│   └── ...                # 其他辅助文件
├── package.json           # 前端依赖与脚本
├── tsconfig.json          # TypeScript 配置
├── vite.config.ts         # Vite 配置
└── ...                    # 其他配置文件
```

### 核心目录说明

- **`api/`** - 后端 API 请求封装
  - `seoApi.ts` - SEO 诊断相关 API
  - `homeContentApi.ts` - 首页内容获取 API

- **`components/`** - 可复用 UI 组件
  - `UrlInput.tsx` - URL 输入组件
  - `SeoResult.tsx` - 基础信息诊断结果展示
  - `SeoElementsResult.tsx` - SEO 要素诊断结果展示
  - `TechSeoResult.tsx` - 技术 SEO 诊断结果展示
  - `AccessibilityResult.tsx` - 可访问性诊断结果展示
  - `ErrorDrawer.tsx` - 错误信息抽屉
  - `Loading.tsx` - 加载动画组件
  - `Empty.tsx` - 空状态组件

- **`pages/`** - 页面级组件
  - `HomePage.tsx` - 首页（输入 URL 开始诊断）
  - `ResultPage.tsx` - 结果页（展示诊断报告）

- **`context/`** - 全局状态管理
  - `DiagnoseContext.tsx` - 诊断结果全局状态

- **`assets/`** - 静态资源（图片、logo 等）

## 🔧 配置说明

### 环境变量

创建 `.env` 文件配置后端 API 地址：

```env
VITE_API_BASE_URL=http://localhost:3001
```

### TypeScript 配置

项目使用 TypeScript 进行类型检查，配置文件：
- `tsconfig.json` - TypeScript 编译配置
- `env.d.ts` - 环境变量类型定义

### Vite 配置

`vite.config.ts` 中配置了：
- React 插件
- 开发服务器代理（如需要）
- 构建优化选项

## 🎨 组件使用示例

### 使用诊断上下文

```tsx
import { useDiagnose } from './context/DiagnoseContext';

function MyComponent() {
  const { diagnoseResult, setDiagnoseResult } = useDiagnose();

  // 使用诊断结果
  return <div>{diagnoseResult?.score}</div>;
}
```

### 调用 API

```tsx
import { fetchBasicInfo } from './api/seoApi';

const result = await fetchBasicInfo('https://example.com');
```

## 🐛 开发调试

### 运行 ESLint

```bash
npm run lint
```

### 类型检查

```bash
npx tsc --noEmit
```

## 📱 浏览器支持

- Chrome >= 87
- Firefox >= 78
- Safari >= 14
- Edge >= 88

## 🔗 相关链接

- [Vite 文档](https://vitejs.dev/)
- [React 文档](https://react.dev/)
- [TypeScript 文档](https://www.typescriptlang.org/)
- [Ant Design](https://ant.design/)

## 📝 开发规范

- 使用 TypeScript 编写代码，确保类型安全
- 组件使用函数式组件 + Hooks
- 遵循 ESLint 规则
- 使用语义化的组件和变量命名
- API 调用统一在 `api/` 目录中封装

## 📄 许可证

ISC

## 👥 作者

Frontend Team

---

**注意：** 本项目需要与后端服务配合使用，后端项目位于 `../backend` 目录。启动前请确保后端服务已经运行在 `http://localhost:3001`。
